import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.executelabs.execute',
  appName: 'Execute',
  webDir: 'dist',
  backgroundColor: '#f6f2e8',
  ios: {
    backgroundColor: '#f6f2e8',
  },
};

export default config;

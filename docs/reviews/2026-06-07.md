# Deep Review — ALL HISTORY — 2026-06-07

Window: all sessions (decision weeks 2026-W22 → W24, ~2026-05-26 to 2026-06-08)
Engine run: `.analysis/all_20260607_215515`

## Headline metrics

| Metric | Value |
|---|---|
| Sessions | 19 |
| Turns | 5,754 (1,449 user · 2,270 assistant) |
| Tool calls | 1,174 |
| Compactions | 0 |
| Retry loops | 3 |
| Error clusters | 35 |
| Est. cost | ~$491.72 (Opus list price; ~89% is context caching) |

**Time:** 955.8 active min (~16h) vs 9,860.6 idle min — a **~10:1 idle:active ratio**, median session 30 min. Short, widely-spaced sessions mean context is cold at almost every start.

**Tokens (exact):** input 38,934 · output 696,808 · cache-write 9,960,581 · cache-read 168,075,199. Cache reads alone are 94% of all tokens.

**Models:** 100% Opus — claude-opus-4-7 (1,121 turns) + claude-opus-4-8 (1,149 turns). No Sonnet/Haiku.

**Tool mix:** Read 288 · Bash 282 · Edit 253 · Grep 149 · TaskOutput 65 · ToolSearch 59 · Glob 37 · Write 9 · Agent 5.
**Bash mix:** npm 38 · cd 32 · curl 27 · ls 25 · git 23 · pkill 17 · npx 15 · sleep 11 · kill 8 · lsof 7. The curl/pkill/sleep/kill/lsof cluster (70 calls) is almost entirely dev-server wrangling.

---

## Top 3 changes to use Claude Code better

### 1. Persist the RevenueCat/IAP architecture into CLAUDE.md — it was re-derived from scratch in 4 separate sessions
This is the single largest cost sink in the whole history, and every analyst hit it independently:
- **Continuity:** the "paywall tap does nothing" symptom was diagnosed, instrumented, and re-explained across sessions `92adaef1`, `1782a572`, `0137c041`, `9b3de0ed` (2026-05-27 → 06-04). Session `0137c041` injected a 17,000-char summary just to rebuild context.
- **Code-health / friction:** `paymentClient.js` edited **21×** (re-read 12×), `revenuecat.js` **18×** (re-read 10×) — the model re-reads before every edit because the iOS-vs-web branch contract isn't written down.
- **Error:** "String to replace not found" twice in exactly these two files (stale anchors from churn), plus the webhook curl retried 3× and `ios:sync` retried 3×.

**Action:** add a RevenueCat block to CLAUDE.md with the package IDs (`$rc_monthly`→`execute_premium_month`, `$rc_annual`→`execute_premium_annual`), entitlement gate `premium`, offering `default`, the OR-gate unlock rule (unlock if Supabase row premium/active **OR** live `customerInfo.entitlements.active` non-empty; device never writes `user_subscription`; webhook is sole writer), and the "configured natively in AppDelegate, not JS" decision. This alone should remove several full sessions of re-tracing.

### 2. Drop the Vite dev-server + curl-polling from the iOS build flow, and document the one config fix
- **Friction + Error:** curl exit-28 (HTTP 000) appears 8+ times with escalating waits (5s→120s), `sleep` 11×, and `lsof`/`pkill`/`kill` 32× — all spent waiting on a local server that doesn't need to exist for an iOS build. One retry loop ran `npm run build … && npx cap copy ios` **5×** in session `92adaef1`.
- **Continuity:** the dev-server hang was re-investigated in 3 sessions (`704beaa9`, `288988c4`, `007980a4`); `vite.config.js` was edited **9×** (7× in one hour) circling the same `optimizeDeps`/`holdUntilCrawlEnd` territory. The working fix — `optimizeDeps.holdUntilCrawlEnd: false` — was found but never written down.

**Action:** CLAUDE.md rule — iOS pipeline is `npm run build` → `npx cap copy ios` → Xcode; **never** start `npm run dev` or curl-probe a dev server as part of a build/sync. Record the `holdUntilCrawlEnd: false` fix so it's never rediscovered.

### 3. Adopt two cheap tool-discipline guardrails (kills ~12 avoidable errors for free)
- **Read-before-Edit:** 8 "File has not been read yet" hard-rejects, each forcing an extra round-trip; 61 `Edit→Read` bigrams confirm edit-then-reorient churn.
- **Scoped Grep:** 4 ripgrep 20s timeouts out of 149 Grep calls — broad searches hitting `node_modules` from repo root.

**Action:** CLAUDE.md rules — always Read a file in-session before Edit; scope every Grep to `src/…` or a filename (or add a `.rgignore` excluding `node_modules`/`dist`). Plus the small self-inflicted ones: `capacitor.config.ts` is `.ts` not `.js` (3 failed opens), and macOS zsh has no GNU `timeout` (use the Bash tool's `timeout` param).

---

## How the project is shaping up

**Changelog (W22–W24):**
- **RevenueCat IAP, full stack** — built the iOS purchase flow end to end: `revenuecat.js` + `paymentClient.js` facade, native `Purchases.configure` in `AppDelegate.swift`, `supabase/functions/revenuecatWebhook/index.ts` for server-side events, paywall + ApplePayButton wired through the facade.
- **Subscription gate** — `useSubscription.js` / `subscription.js` consume live entitlement state; `PremiumGate.jsx` gates premium pages; `AuthContext.jsx` (10 edits) calls RC `logIn` on Supabase auth to tie identities.
- **App Store compliance** — `NSCameraUsageDescription` added; `PrivacyInfo.xcprivacy` collected-data types filled (Email, UserID, PurchaseHistory, Health, Fitness, PhotosOrVideos); Terms governing law set to British Columbia, Canada; paywall now pulls live StoreKit prices + Restore + policy links; placeholder integrations removed from `Profile.jsx`.
- **Nutrition + personalization** — `Nutrition.jsx` heavily reworked (23 edits, biggest burst 06-08); `personalizationSync.js`/`goalSync.js`/`generateInitialPlanBundle.js`/`refinePlanFromChat.js` aligned to updated profile fields.

**Key decisions:**
- RC configured **natively in AppDelegate**, not from JS — avoids cold-launch race on first bridge call.
- **Platform-aware facade** — iOS→RevenueCat/StoreKit, web→Stripe — required by Guideline 3.1.1.
- **Supabase webhook = server-authoritative** subscription state; client-only entitlement checking rejected.
- **Health + Fitness kept** in PrivacyInfo (nutrition=Health, workouts=Fitness) — must match ASC Nutrition Label.
- **Bundled web assets, no `server.url`** — Guideline 4.2.

**Open threads / risks:**
- ⚠️ `src/pages/Nutrition.jsx` is **unstaged and dirty** with an 11-edit burst on 06-08 — highest-churn file, stability of in-flight changes unknown.
- ⚠️ **Webhook writeback unconfirmed live** — curl retried 3× / timed out; the `revenuecatWebhook` edge function may not be deployed or auth header may be wrong. Premium-unlock-after-purchase is not proven end to end.
- ⚠️ `ios:sync` failed 3× in one session — possible SPM/`cap copy` latent issue; no clean Xcode archive observed in logs.
- Human-side App Store blockers remain: ASC Nutrition Label filing, demo review credentials, age rating.
- `/tmp/apple-client-secret.mjs` (W24) suggests SIWA client secret may need regeneration — expiry would break Sign in with Apple in production.

---

## Skill & code-health signals

**Prompting** — capable but inconsistent. The best prompts (the purchase-bug diagnosis with "trace the actual code path… HARD CONSTRAINTS", and the subscription-unlock spec with labelled Change A/Change B + exclusions) are model-grade and eliminated rework. The weak ones are symptom-dumps ("The project is not building. Can you figure out why") and raw Xcode console pastes with no question or diff — these spawned 6–8 turn diagnostic loops. Closing that gap (always state env + what you tried + done-criteria) is the highest-value prompting habit.

**Continuity** — 0 compactions is healthy, but context loss happens *between* sessions, not within them. The June-4 audit day fragmented into 5 sessions in <2h, with the full 10k-char `/appstore-audit` prompt injected and re-run twice across a `/clear` 19 min apart. All work is on `main` — no feature branches even for risky native-bridge work; two worktree-agent branches appeared then vanished with outcomes never surfaced to MEMORY.md.

**Code-health instability hotspots** (edits / re-reads):
- `Nutrition.jsx` 23 / 17 — by far the worst; also overflowed the 25k-token read limit (27,354 tokens). Split it (extract `useNutritionData`, document calorie/macro data-flow).
- `paymentClient.js` 21 / 12 and `revenuecat.js` 18 / 10 — see Top Change #1; freeze the API surface with JSDoc contracts.
- `PremiumPaywall.jsx` 13, `AuthContext.jsx` 10, `LogFood.jsx` 11/8, `AppShell.jsx` 9/6 — secondary churn.
- Pure-read confusion zone: `useSubscription.js` (6 reads, 0 edits), `subscription.js` (5 reads), `revenuecat.js` are read together repeatedly — nobody's sure where subscription truth lives. A one-paragraph "responsibility of each file" note would stop the triangulation.

---

## CLAUDE.md / command additions to make now

Paste-ready CLAUDE.md block:
```
## Agent guardrails
- Read a file in-session before you Edit it. Bare Edit on an unread file is rejected. Read → Edit is mandatory.
- Scope every Grep to a subdir (src/lib/, src/pages/) or a filename. Never grep the repo root loosely. Prefer Read+offset/limit when the target file is known.
- Capacitor config is `capacitor.config.ts` (TypeScript). There is no `.js` variant.
- macOS zsh has no GNU `timeout`. Use the Bash tool's built-in timeout param, not `timeout <n> <cmd>`.
- `Nutrition.jsx` exceeds the single-read token limit — always read it with offset/limit until it's split.

## iOS build pipeline
- Build: `npm run build` → `npx cap copy ios` → open Xcode. Never run `npm run dev` or curl-probe a dev server as part of a build/sync.
- If `npx cap sync ios` fails more than once, stop and run `npx cap doctor`; check ios/App/CapApp-SPM/Package.swift before retrying.
- Vite dev-server hang fix: set optimizeDeps.holdUntilCrawlEnd: false in vite.config.js.

## RevenueCat / IAP (iOS)
- Package IDs: $rc_monthly → execute_premium_month, $rc_annual → execute_premium_annual
- Entitlement gate: "premium"; Offering: "default" (Current in RC dashboard)
- Unlock = OR-gate: Supabase user_subscription row premium/active OR live customerInfo.entitlements.active non-empty.
  Device never writes user_subscription; the revenuecatWebhook edge function is the sole writer.
- SDK configured natively in AppDelegate.swift (not from JS) to avoid cold-launch races.
- Subscription truth lives across: revenuecat.js (SDK/entitlement), subscription.js (backend row), useSubscription.js (React hook). Read order: hook → lib.
```

Slash-command ideas the analysts proposed:
- **`/ios-sync`** — runs `npm run build && npx cap copy ios` and prints a pass/fail summary. Replaces dozens of free-form "build then sync" turns and the 5× retry loop.
- **`/xcode-log`** — formats a pasted Xcode console: strips noise (CFPrefsPlistSource, RTIInputSystemClient, UIScene), highlights Execute-namespaced lines, asks for a specific next action. Replaces ~6 raw-paste turns per RC session.
- **`/iap-status`** — snapshots current RC/StoreKit state (SDK init log, offering name, package IDs, entitlement key) to kill repeated "where is the SDK / what does the dashboard say" turns.

Handoff habit (cheapest continuity win): before every `/clear`, append 3 lines to MEMORY.md — current task, status (done/blocked/in-progress), and the one file most likely needed next session.

---

## Trend vs last runs

Only one engine run exists in `.analysis/trends.sqlite` so far (`all_20260607_215515`: 19 sessions, 5,754 turns, 3 retry loops, 35 error clusters, $491.72). No trend can be computed yet — re-run `/deep-review` after another week or two of work and this section will show whether retry loops, error clusters, and cost-per-session are trending down (the real signal that the workflow is improving).
